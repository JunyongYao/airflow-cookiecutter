use following command to start docker, please change the files based on your real scenario.
 ```bash
docker-compose -f docker-compose.yml -f docker-compose-local.yml -f docker-compose-local.user.yml 
```